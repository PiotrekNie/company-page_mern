import React from "react";
import styled from "styled-components";
import tw from "twin.macro";

interface ButtonProps {
  url: string;
  color: string;
  text: string;
}

export function CTA(props: ButtonProps) {}
